// const { readdirSync, statSync, lstatSync } = require("fs");
// const { join } = require("path");

// const dirs = p => lstatSync(p).filter(f => statSync(join(p, f)).isDirectory());
// const files = p =>
//   lstatSync(p).filter(f => !statSync(join(p, f)).isDirectory());

// const catalogData = {
//   directories: dirs(join(__dirname, "components")).map(name => ({
//     name,
//     files: files(name)
//   }))
// };
{}